## 1.0.1

- Add `.pubignore` to exclude large binary samples, test vectors, certs, and example apps from the published package.
- Remove unused `package:typed_data` import in DTLS `server_name` extension.
- Declare `false_secrets` for self-contained DTLS test fixtures.
- Add `issue_tracker` link.

## 1.0.0

- Initial version.
