name: pure_dart_webrtc_packet_loss_simulator_example
description: Bidirectional UDP middlebox that drops, reorders, and delays packets.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.6.2

dependencies:
  args: ^2.7.0

dev_dependencies:
  lints: ^5.0.0
