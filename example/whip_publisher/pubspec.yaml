name: pure_dart_webrtc_whip_publisher_example
description: >
  Publish a VP8 video file to any WHIP-compatible ingest endpoint
  (Cloudflare Stream, Broadcast Box, OvenMediaEngine, simple-whip-server).
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.6.2

dependencies:
  pure_dart_webrtc:
    path: ../../
  args: ^2.7.0

dev_dependencies:
  lints: ^5.0.0
